import { useEffect, useState } from 'react'
import { TAGLINE } from '../config/site'
export function Navbar({ count, onCart }: { count: number; onCart: () => void }) { const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); useEffect(() => { const f = () => setScrolled(scrollY > 16); addEventListener('scroll', f); return () => removeEventListener('scroll', f) }, []); const links = [['Inicio', 'inicio'], ['Variedades', 'variedades'], ['Nuestra historia', 'historia'], ['Cómo funciona', 'como-funciona'], ['Contacto', 'contacto']]; const go = (id: string) => { document.getElementById(id)?.scrollIntoView({behavior: 'smooth'}); setOpen(false) }; return <header className={`navbar ${scrolled ? 'scrolled' : ''}`}><a className="brand" href="#inicio" aria-label={`PAPAY: ${TAGLINE}`}>PAP<span>A</span>Y<i>•</i></a><nav className={open ? 'nav-open' : ''}>{links.map(([name, id]) => <button key={id} onClick={() => go(id)}>{name}</button>)}</nav><button className="cart-trigger" onClick={onCart} aria-label={`Abrir carrito: ${count} kilos`}><span>Canasta</span><b>{count}</b></button><button className="menu" onClick={() => setOpen(!open)} aria-label="Abrir menú" aria-expanded={open}><i/><i/><i/></button></header> }
